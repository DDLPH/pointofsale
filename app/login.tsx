'use client';
import {useState} from 'react';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
import {Label} from '@/components/ui/label';
export default function Login(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="signin"><div className="brand-mark">ไก่</div><h1>ก๋วยเตี๋ยวไก่มะระ</h1><p>เข้าสู่ระบบร้านค้า</p><form style={{width:'100%',textAlign:'left'}} onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'login',username:f.get('username'),password:f.get('password')})});const d=await r.json() as {error?:string};if(!r.ok)throw new Error(d.error||'เข้าสู่ระบบไม่ได้');window.location.assign('/');}catch(e){setError(e instanceof Error?e.message:'เชื่อมต่อไม่ได้');setBusy(false);}}}><Label htmlFor="username">ชื่อผู้ใช้</Label><Input id="username" name="username" autoComplete="username" autoCapitalize="none" spellCheck={false} required maxLength={40}/><Label htmlFor="password">รหัสผ่าน</Label><Input id="password" name="password" type="password" autoComplete="current-password" required maxLength={128}/>{error&&<p role="alert" className="dialog-error">{error}</p>}<Button type="submit" disabled={busy}>{busy?'กำลังเข้าสู่ระบบ…':'เข้าสู่ระบบ'}</Button></form></main>;}
