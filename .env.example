# Set only for server-side runtime. Each hash must be independently salted.
POS_BOOTSTRAP_USERS='[{"username":"Admin1","role":"admin","hash":"<salted PBKDF2 hash>"},{"username":"P01","role":"staff","hash":"<salted PBKDF2 hash>"}]'
# Google Sheets: configure the real values as server secrets, never in client code.
GOOGLE_SHEETS_PULL_SECRET=
