import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const products = sqliteTable('products', { id: text('id').primaryKey(), owner: text('owner').notNull(), name: text('name').notNull(), price: integer('price').notNull(), category: text('category').notNull(), active: integer('active').notNull().default(1) }, t => [index('products_owner').on(t.owner)]);
export const orders = sqliteTable('orders', { id: text('id').primaryKey(), owner: text('owner').notNull(), created: text('created').notNull(), total: integer('total').notNull(), method: text('method').notNull(), received: integer('received').notNull(), items: text('items').notNull(), cancelled: text('cancelled'), reason: text('reason') }, t => [index('orders_owner_created').on(t.owner,t.created)]);
export const staffUsers = sqliteTable('staff_users', {id:text('id').primaryKey(),username:text('username').notNull().unique(),passwordHash:text('password_hash').notNull(),role:text('role').notNull(),active:integer('active').notNull().default(1)});
export const sessions = sqliteTable('sessions', {tokenHash:text('token_hash').primaryKey(),userId:text('user_id').notNull().references(()=>staffUsers.id),expires:integer('expires').notNull()}, t=>[index('sessions_user').on(t.userId),index('sessions_expiry').on(t.expires)]);
export const loginLimits = sqliteTable('login_limits', {key:text('key').primaryKey(),windowStart:integer('window_start').notNull(),attempts:integer('attempts').notNull()});
